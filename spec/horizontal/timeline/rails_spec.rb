require 'spec_helper'

describe Horizontal::Timeline::Rails do
  it 'has a version number' do
    expect(Horizontal::Timeline::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
